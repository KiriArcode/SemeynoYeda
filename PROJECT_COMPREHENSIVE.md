# SemeynoYeda — Полная документация проекта

**Версия:** 1.0.0  
**Дата:** Февраль 2026  
**Тип:** PWA-приложение для управления семейным питанием

---

## Оглавление

1. [Обзор проекта](#обзор-проекта)
2. [Технологический стек](#технологический-стек)
3. [Архитектура](#архитектура)
4. [Модель данных](#модель-данных)
5. [Дизайн-система](#дизайн-система)
6. [Функциональность](#функциональность)
7. [Пользовательские сценарии](#пользовательские-сценарии)
8. [Разработка и деплой](#разработка-и-деплой)
9. [Особенности реализации](#особенности-реализации)
10. [Roadmap](#roadmap)

---

## Обзор проекта

### Назначение

**SemeynoYeda** — Progressive Web Application (PWA) для управления семейным питанием с учётом индивидуальных диетических потребностей. Приложение работает полностью офлайн после первой загрузки и оптимизировано для мобильных устройств.

### Ключевые особенности

- 🗓️ **Планирование меню на неделю** с учётом диетических ограничений
- 📖 **Каталог рецептов** с фильтрацией по категориям, тегам и участникам семьи
- 🛒 **Автоматическая генерация списка покупок** из запланированных блюд
- 🧊 **Трекер морозилки** с отслеживанием порций и сроков годности
- 🧑‍🍳 **Режим повара** с чек-листами заготовок и параллельной готовкой
- 📱 **Полностью офлайн** (PWA + IndexedDB)
- 🎨 **Тёмная тема "Dimension Kitchen"** в sci-fi стиле

### Целевая аудитория

- **Коля** — 36 лет, гастрит, лишний вес, работает из дома. Нужна щадящая еда (`gastritis-safe`). Основные способы: пароварка, аэрогриль (без масла), тушение.
- **Кристина** — 34 года, лишний вес, работает в офисе. Утром кофе, на завтрак времени нет. Обед берёт с собой или покупает. Основные способы: духовка (корочка), электрогриль, сковорода.
- **Общее**: продукты закупаются раз в неделю. Раз в неделю — prep day (заготовки: фарш по пакетам, котлеты, фрикадельки, соусы).

### Проблема и решение

**Проблема:**
- Планирование меню на неделю с учётом индивидуальных диет
- Отслеживание заготовок в морозилке и их сроков годности
- Составление списков покупок из запланированных блюд
- Управление рецептами с вариациями для разных членов семьи
- Организация процесса заготовок на выходные дни

**Решение:**
PWA-приложение с offline-first подходом, которое объединяет все эти функции в единый интерфейс, работающий полностью офлайн.

---

## Технологический стек

### Frontend Framework

**React 18 + TypeScript 5**
- Версия: React 18.3.0, TypeScript 5.3.3
- Режим: strict mode
- Архитектура: функциональные компоненты с хуками
- Роутинг: React Router 6.20.0

**Почему выбрано:**
- Компонентная архитектура идеально подходит для модульной структуры
- Широкая экосистема и сообщество
- Отличная поддержка TypeScript
- Concurrent rendering для плавной работы UI

### Сборщик и инструменты разработки

**Vite 5**
- Версия: 5.0.8
- Плагины: `@vitejs/plugin-react`, `@tailwindcss/vite`, `vite-plugin-pwa`
- Особенности:
  - Мгновенный HMR (Hot Module Replacement)
  - Нативная поддержка PWA через плагины
  - Быстрая сборка благодаря esbuild
  - Оптимизация production бандла

### Стилизация

**Tailwind CSS 4**
- Версия: 4.x (через @tailwindcss/vite)
- Подход: Utility-first CSS
- Кастомная тема через CSS переменные
- Минимальный размер бандла благодаря tree-shaking

**CSS переменные:**
- Фоны: void (#0B0E14), panel (#111620), card (#161C2A), elevated (#1E2538), nebula (#252D3B)
- Акценты: portal (#39FF14), portal-dim (#00E676)
- Семья: kolya (#00E5FF), kristina (#FF6B9D), both (#39FF14)
- Еда: ramen, miso, matcha, sakura, frost, plasma

### Хранение данных

**Neon PostgreSQL** (serverless) — основная база данных, доступ через Vercel Serverless Functions.

**Dexie.js 3.2.4** (IndexedDB) — локальный кэш для offline работы.

- TypeScript поддержка
- Реактивные хуки для React
- Миграции схемы данных
- Синхронизация с Neon через `syncService.ts`

**Структура БД (таблицы в Neon и IndexedDB):**
- `recipes` — рецепты
- `menus` — недельные меню
- `freezer` — заготовки в морозилке
- `shopping` — списки покупок
- `prepPlans` — планы заготовок
- `cookingSessions` — сессии готовки
- `chefSettings` — настройки режима повара

### PWA функциональность

**vite-plugin-pwa (Workbox)**
- Service Worker для offline работы
- Манифест PWA
- Стратегии кэширования:
  - App shell: Cache First (HTML, CSS, JS)
  - Данные: Stale While Revalidate
  - Изображения: Cache First с лимитом 50MB

### Иконки

**Lucide React 0.294.0**
- Единый стиль всех иконок
- Tree-shakeable (только используемые иконки)
- TypeScript поддержка
- Легковесные SVG иконки

### Утилиты

**nanoid 5.0.4**
- Генерация уникальных ID
- URL-safe ID
- Быстрее чем UUID

### Хостинг и деплой

**Vercel**
- base: '/'
- Автоматический деплой при push в main
- HTTPS обязателен для PWA функциональности

### Backend

**Neon Serverless PostgreSQL** — основная база данных. Доступ через Vercel Serverless Functions (`api/` директория).

- REST API: `api/data/[[...resource]].ts`
- Offline-first синхронизация через `syncService.ts` (базовая реализация)
- Репозитории: `api/_lib/repositories/`

---

## Архитектура

### Структура проекта

```
SemeynoYeda/
├── .cursor/rules/              # Правила Cursor AI
│   ├── design-system.mdc      # Дизайн-система (alwaysApply)
│   ├── features-and-docs.mdc   # Документация
│   └── parse-recipes.mdc       # Парсинг рецептов
├── public/
│   ├── icons/                  # PWA иконки
│   └── 404.html                # SPA fallback для роутинга
├── src/
│   ├── app/
│   │   └── Router.tsx          # Маршруты и конфигурация роутера
│   ├── components/
│   │   ├── ui/                 # Modal, Toast, AlertBanner
│   │   ├── layout/             # PageShell, BottomNav, Breadcrumbs
│   │   ├── menu/               # WeekOverview, MealSlot, SwapModal
│   │   ├── recipe/             # RecipeForm
│   │   ├── prep/               # PrepBlock, PrepTaskCard
│   │   ├── cooking/            # CookingSession, IngredientCheck
│   │   └── shopping/           # ShoppingSettings
│   ├── data/
│   │   ├── schema.ts           # TypeScript типы данных
│   │   ├── seedRecipes.ts      # Seed-рецепты
│   │   └── seedMenu.ts         # Seed-меню
│   ├── hooks/                  # Custom hooks
│   │   ├── useShoppingList.ts
│   │   ├── usePrepPlan.ts
│   │   ├── useBatchCooking.ts
│   │   ├── useCookingTimers.ts
│   │   ├── useFreezerAlerts.ts
│   │   ├── useIngredientAvailability.ts
│   │   └── useToast.ts
│   ├── lib/
│   │   ├── db.ts               # Dexie.js инстанс
│   │   ├── initDb.ts           # Инициализация БД
│   │   └── dataService.ts     # Сервис работы с данными
│   ├── pages/                  # Страницы приложения
│   │   ├── MenuPage.tsx
│   │   ├── RecipesPage.tsx
│   │   ├── RecipeDetailPage.tsx
│   │   ├── RecipeEditPage.tsx
│   │   ├── RecipeNewPage.tsx
│   │   ├── ShoppingPage.tsx
│   │   ├── FreezerPage.tsx
│   │   ├── PrepPage.tsx
│   │   ├── CookingPage.tsx
│   │   └── ChefSettingsPage.tsx
│   ├── styles/
│   │   └── globals.css         # CSS переменные, базовые стили
│   └── main.tsx
├── api/                        # Vercel API routes (опционально)
│   ├── _lib/
│   │   ├── db.ts               # Neon connection
│   │   └── repositories/       # Репозитории для работы с БД
│   └── data/
│       └── [[...resource]].ts  # REST endpoint
├── scripts/                    # Утилиты
│   ├── seedDb.ts               # Seed IndexedDB
│   ├── seedSupabase.ts         # Seed Supabase
│   └── checkDb.ts              # Проверка БД
├── supabase/
│   └── migrations/             # SQL миграции
├── cursorrules                 # Глобальные правила Cursor
├── index.html
├── vite.config.ts
├── tailwind.config.ts
├── tsconfig.json
├── package.json
└── README.md
```

### Потоки данных

```
┌──────────────────────────────────────────────────────────────────┐
│                         DATA FLOW                                │
│                                                                  │
│  ┌─────────────────┐   seed scripts     ┌───────────────────┐   │
│  │ seedRecipes.ts  │──────────────────▶│  Neon PostgreSQL  │   │
│  │ seedMenu.ts     │                    │  (основная БД)    │   │
│  └─────────────────┘                    └────────┬──────────┘   │
│                                                  │               │
│                                    syncService.ts│↕ sync        │
│                                                  │               │
│  ┌──────────┐   dataService.ts    ┌──────────────┴──────────┐   │
│  │    UI    │◀──────────────────▶│  IndexedDB (Dexie)      │   │
│  │          │   fast read/write   │  (локальный кэш)        │   │
│  └──────────┘                     └─────────────────────────┘   │
│                                                                  │
│  Запись: optimistic → IndexedDB → push в Neon                   │
│  Чтение: UI → dataService → IndexedDB (быстрое)                │
│  Фон: syncService → Neon ↔ IndexedDB (двусторонняя синхр.)     │
└──────────────────────────────────────────────────────────────────┘
```

**Цикл работы:**
1. **Старт приложения** — initDb: открытие Dexie, syncSeedRecipes (bulkPut seed-рецептов и seed-меню в IndexedDB)
2. **Runtime** — чтение из IndexedDB (быстрое), запись optimistic в IndexedDB с последующим push в Neon
3. **Фоновая синхронизация** — syncService.ts обеспечивает двустороннюю синхронизацию Neon ↔ IndexedDB

### Архитектурные принципы

**Offline-First подход:**
- Основные данные хранятся в Neon PostgreSQL (serverless)
- IndexedDB (Dexie) используется как локальный кэш для offline работы
- Service Worker кэширует все ресурсы
- Стартовые рецепты и меню загружаются из seed при инициализации
- Запись: optimistic в IndexedDB → push в Neon
- syncService.ts обеспечивает двустороннюю синхронизацию

**Seed + Neon + IndexedDB:**
- Seed-рецепты и seed-меню в `src/data/seedRecipes.ts` и `src/data/seedMenu.ts`
- При старте приложения initDb вызывает syncSeedRecipes (bulkPut в IndexedDB)
- Чтение данных — из IndexedDB (быстрое), синхронизация с Neon в фоне
- Запись — optimistic в IndexedDB, затем push в Neon через API

**Компонентная архитектура:**
- Один компонент — одна ответственность
- Переиспользование через props
- Композиция вместо наследования

---

## Модель данных

### Основные типы

**FamilyMember:**
```typescript
type FamilyMember = 'kolya' | 'kristina' | 'both';
```

**DietTag:**
```typescript
type DietTag =
  | 'gastritis-safe'      // щадящее для гастрита
  | 'soft-texture'        // мягкая текстура
  | 'rich-feel'           // ощущение "богатой" еды
  | 'freezable'           // можно заморозить
  | 'quick'               // <15 минут
  | 'prep-day'            // заготовка выходного дня
  | 'batch-cooking'       // массовая готовка + фасовка
  | 'overnight'           // можно приготовить с вечера
  | 'packable'            // можно взять с собой
  | 'low-calorie'         // низкокалорийное
  | 'blanch-before-freeze'; // бланширование перед заморозкой
```

**MealType:**
```typescript
type MealType =
  | 'breakfast'
  | 'second_breakfast'    // второй завтрак (5-6 приёмов)
  | 'lunch'
  | 'snack'
  | 'dinner'
  | 'late_snack';         // второй ужин (5-6 приёмов)
```

### Основные сущности

**Recipe:**
- `id`: string (nanoid)
- `slug`: string (url-friendly имя)
- `title`: string
- `subtitle?`: string
- `category`: 'main' | 'sauce' | 'side' | 'breakfast' | 'snack' | 'soup' | 'dessert'
- `tags`: DietTag[]
- `suitableFor`: FamilyMember
- `prepTime`: number (минуты)
- `cookTime`: number (минуты)
- `totalTime`: number (минуты)
- `servings`: number
- `ingredients`: Ingredient[]
- `steps`: RecipeStep[]
- `equipment`: EquipmentId[]
- `notes?`: string
- `storage`: { fridge?: number, freezer?: number, vacuumSealed?: boolean }
- `reheating?`: ReheatingInstruction[]
- `version?`: number
- `source?`: string
- `imageUrl?`: string
- `createdAt`: string (ISO date)
- `updatedAt`: string (ISO date)

**WeekMenu:**
- `id`: string
- `weekStart`: string (ISO date понедельника)
- `days`: MenuDay[]
- `shoppingListGenerated`: boolean
- `shoppingSettings?`: ShoppingSettings
- `shoppingDay?`: string (ISO date дня покупок)
- `createdAt`: string

**FreezerItem:**
- `id`: string
- `recipeId`: string
- `name`: string
- `portions`: number (обратная совместимость)
- `portionsRemaining`: number
- `portionsOriginal`: number
- `batchId?`: string (ID партии заготовки)
- `frozenDate`: string (ISO date)
- `expiryDate`: string (ISO date)
- `location?`: string
- `forWhom?`: FamilyMember
- `reheating?`: ReheatingInstruction[]

**ShoppingItem:**
- `ingredient`: string
- `totalAmount`: number
- `unit`: Unit
- `category`: 'мясо' | 'молочка' | 'овощи' | 'крупы' | 'другое'
- `checked`: boolean
- `recipeIds`: string[]
- `markedMissing?`: boolean
- `markedAt?`: string
- `source`: 'auto' | 'manual' | 'missing'
- `coveredByFreezer?`: boolean

**PrepPlan:**
- `id`: string
- `date`: string (ISO date)
- `tasks`: PrepTask[]
- `estimatedTime`: number (минуты)
- `completedTasks`: string[]

**CookingSession:**
- `id`: string
- `date`: string (ISO date)
- `mealType`: MealType
- `recipes`: string[] (ID рецептов)
- `timers`: CookingTimer[]
- `startedAt?`: string (ISO date)
- `completedAt?`: string (ISO date)

**ChefModeSettings:**
- `id`: string
- `enabled`: boolean
- `showPrepBlock`: boolean
- `showParallelCooking`: boolean
- `defaultPrepTime`: number (минуты до готовки)
- `kolyaMealsMode?`: '4' | '5-6'

### Индексы IndexedDB

```typescript
recipes: 'id, slug, category, *tags, suitableFor'
menus: 'id, weekStart, shoppingDay, createdAt'
freezer: 'id, recipeId, expiryDate, batchId'
shopping: 'ingredient, category, checked, markedMissing'
prepPlans: 'id, date'
cookingSessions: 'id, date, mealType'
chefSettings: 'id'
```

---

## Дизайн-система

### Философия дизайна

**Концепция: "Портал Рика ведёт на кухню"**

Тёмный void как основа. Портальный зелёный (#39FF14) — навигация и интерактив. Тёплые «фудовые» цвета — контент о еде. Чистые линии. Sci-fi типографика.

**Три столпа:**
- **Dark Foundation** — глубокий космический фон, 5 уровней поверхностей
- **Portal Navigation** — зелёный портал = всё интерактивное
- **Food Warmth** — контент о еде окрашен тёплыми цветами

### Цветовая палитра

**Фоны (5 уровней глубины):**
- `--void: #0B0E14` — основной фон body
- `--panel: #111620` — sticky header, bottom nav
- `--card: #161C2A` — карточки, аккордеоны
- `--elevated: #1E2538` — модалки, dropdown, hover
- `--nebula: #252D3B` — бордеры, разделители

**Portal (основной акцент):**
- `#39FF14` — portal-green
- `#00E676` — portal-dim (hover, secondary)
- `rgba(57,255,20,0.12)` — portal-glow (box-shadow)
- `rgba(57,255,20,0.05)` — portal-soft (bg активных)

**Акценты:**
- Кристина / anime: `#FF6B9D` (accent-pink)
- Коля / tech: `#00E5FF` (accent-cyan)
- Таймеры / предупреждения: `#FF9100` (accent-orange)
- Заготовки / prep: `#B388FF` (accent-purple)

**Семья — цвета участников:**
- **Коля**: main `#00E5FF`, bg `rgba(0,229,255,0.08)`, border `rgba(0,229,255,0.2)`
- **Кристина**: main `#FF6B9D`, bg `rgba(255,107,157,0.08)`, border `rgba(255,107,157,0.2)`
- **Оба**: main `#39FF14`, bg `rgba(57,255,20,0.08)`, border `rgba(57,255,20,0.2)`

**Теги рецептов:**
- gastritis-safe: bg `#0D2818`, text `#39FF14`, border `#1A4030`
- freezable: bg `#0D1B28`, text `#00E5FF`, border `#1A3040`
- quick: bg `#281A0D`, text `#FF9100`, border `#403020`
- prep-day: bg `#1A0D28`, text `#B388FF`, border `#2D1A40`
- batch-cooking: bg `#1A1A28`, text `#B388FF`, border `#2D2D40`
- rich-feel: bg `#280D1A`, text `#FF6B9D`, border `#401A2D`

### Типографика

**Шрифты:**
- Heading: `'Exo 2'`, fallback `'Chakra Petch'`
- Mono: `'Share Tech Mono'`, fallback `'JetBrains Mono'`
- Body: `'DM Sans'`, fallback `'Noto Sans JP'`
- **ЗАПРЕЩЕНЫ**: Inter, Roboto, Arial, system-ui

**Шкала:**
- Page Title: heading 36px w900 ls:-0.5px
- Section Title: heading 22px w800
- Card Title: heading 16px w800 lh:1.2
- Body: heading 13px w500 (text-secondary)
- Data / Mono: mono 11px (text-muted)
- Dimension Label: mono 10px ls:1.5px uppercase (portal-dim)
- Tag: heading 11px w600 ls:0.3px
- Button: heading 13px w700 ls:0.3px

### Компоненты

**Badge (участник):**
Тонкий бейдж с цветной точкой и свечением. Три варианта: Коля (cyan), Кристина (pink), Оба (portal green).

**Tag (тег рецепта):**
Компактный тег с фоновой подложкой. Каждый тег имеет уникальный bg/text/border набор.

**Pill (фильтр-кнопка):**
Переключатель-pill для фильтров и табов. Active: `bg: portal-glow`, `color: portal`. Inactive: `bg: transparent`, `color: muted`.

**MealSlot (аккордеон):**
Вместо всех приёмов раскрытых — аккордеон: один приём развёрнут, остальные свёрнуты. Каждый элемент внутри приёма имеет цветную полоску слева (цвет участника).

**WeekOverview:**
Горизонтальная полоска 7 дней. Точки (●) = заполненные приёмы. Активный день = portal фон. Prep days (Сб/Вс) = 📦 иконка + фиолетовый текст.

**Stats Bar:**
Горизонтальная полоска с иконками, подписями и значениями: ⏱ Время, 🍽 Порции, 🧊 Холод., ❄️ Мороз.

**Equipment Card:**
Карточка оборудования с иконкой и настройкой (например, "Плита — средний огонь").

**Prep Checklist:**
Чек-лист с прогрессом, разбитый по часам/фазам. Header фазы: mono label (`ЧАС 1 · ФАРШ`), heading title, счётчик done/total, progress bar.

### Навигация

**Bottom Nav:**
4 таба max, rounded-[20px] контейнер, portal-soft активный фон. Active: иконка без фильтра, portal green текст, зелёная точка/полоска снизу с glow. Inactive: иконка grayscale 60% + opacity 40%, muted текст.

**Chef Mode:**
НЕ отдельные табы — floating overlay снизу. Показывает текущий шаг, таймер, прогресс, "Далее →".

**Touch targets:**
>= 44×44px (Apple HIG)

### Анимации

- Default transition: `all 0.25s cubic-bezier(0.4, 0, 0.2, 1)`
- Card appear: slideUp с stagger delay `calc(var(--i) * 60ms)`
- Accordion: slideDown 0.2s
- Max duration: 0.4s (кроме фоновых блобов)

### Модалки

- Рендерить через `createPortal(modal, document.body)`
- z-index: 9999
- backdrop-filter: blur(12px)
- Закрытие: клик overlay, X, Escape

### Запрещено

- Светлые/белые фоны
- Inter, Roboto, Arial, system-ui
- Inline styles (только Tailwind, исключение — gap в старых компонентах)
- Анимации > 0.4s (кроме блобов)
- Touch targets < 44×44px
- Пастельные тона
- border-radius < 4px
- localStorage (только IndexedDB через Dexie)
- Серверный рендеринг

---

## Функциональность

### Реализованные функции

**Навигация и оболочка:**
- React Router с lazy-загрузкой страниц
- PageShell: header «SemeynoYeda» + ChefModeToggle
- BottomNav: 4 таба (Меню / Рецепты / Морозилка / Покупки)
- Режим повара: переключатель с optimistic update
- Breadcrumbs: кнопка «Назад» + цепочка навигации
- Portal Blobs: декоративные анимированные блобы на фоне

**Страницы:**
- `/` — MenuPage: Недельное меню с WeekOverview и аккордеоном MealSlot
- `/recipes` — RecipesPage: Каталог рецептов с поиском и фильтрами
- `/recipe/:id` — RecipeDetailPage: Детальная карточка рецепта со Stats Bar и Equipment Cards
- `/shopping` — ShoppingPage: Список покупок с авто-генерацией из меню
- `/freezer` — FreezerPage: Морозилка с трекингом порций и сроков
- `/prep` — PrepPage: Заготовки с фазами, progress bar, counter badge
- `/cooking` — CookingPage: Готовка с выбором даты и приёма, запуск сессии
- `/settings/chef` — ChefSettingsPage: Настройки режима повара

**Компоненты:**
- WeekOverview: горизонтальная полоска 7 дней с точками-индикаторами
- MealSlot: аккордеон с цветными полосками участников
- Breadcrumbs: mono шрифт, muted цвет
- IngredientCheck: проверка наличия ингредиентов
- CookingSession: многошаговая сессия готовки
- ParallelCooking: активные таймеры с пауза/стоп
- Modal, Toast, ChefModeToggle, AlertBanner: UI-компоненты
- SwapModal: замена блюда в меню

**Данные и синхронизация:**
- **Neon PostgreSQL** — основное хранилище данных (serverless)
- **Dexie (IndexedDB)** — локальный кэш: таблицы recipes, menus, freezer, shopping, prepPlans, cookingSessions, chefSettings
- **syncService.ts** — двусторонняя синхронизация Neon ↔ IndexedDB
- syncSeedRecipes: при каждом запуске bulkPut всех seed-рецептов и seed-меню
- 57 seed-рецептов: соусы, котлеты, фрикадельки, суфле, пюре, крупы, завтраки, полдники, супы, гарниры и другие
- Разделение: Коля / Кристина / Оба — в рецептах и в меню

**Хуки:**
- `useShoppingList` — генерация и управление списком покупок
- `useIngredientAvailability` — отметки наличия ингредиентов
- `useBatchCooking` — план batch cooking: генерация, toggle задач
- `usePrepPlan` — план подготовки по рецептам и дате
- `useCookingTimers` — таймеры готовки (старт/пауза/стоп)
- `useFreezerAlerts` — предупреждения о морозилке
- `useToast` — уведомления

### Планируемые функции

**Фаза 1 — PWA и CRUD рецептов:**
- PWA (offline-first) с vite-plugin-pwa + Workbox Service Worker
- CRUD рецептов: форма добавления, редактирование, удаление
- Замена блюда в меню через SwapModal
- Версионирование seed-данных

**Фаза 2 — Замороженные компоненты:**
- Расширение FreezerItem: portionsRemaining, portionsOriginal, batchId
- Расширение MealSlot: usesFromFreezer
- UI: индикатор снежинки "из морозилки" в MealSlot
- Shopping list: вычитать ингредиенты, покрытые замороженными компонентами
- Инструкции разогрева по персоне

**Фаза 3 — Prep Day (улучшения):**
- Список покупок для prep day
- Реальные таймеры с Vibration API
- Quick-swap из последних рецептов

**Фаза 4 — UX для реального дня:**
- Завтрак: тег overnight, фильтр "быстрые завтраки"
- Обед Кристины: тег packable
- Разогрев для Коли: инструкции с учётом оборудования
- Фильтр "Мои блюда"
- Chef Mode Overlay: floating overlay внизу экрана

**Фаза 5 — Умные подсказки и статистика:**
- Предупреждения о заканчивающихся заготовках
- Статистика разнообразия меню
- Баланс категорий

**Фаза 6 — Шаблоны и экспорт данных:**
- Шаблоны недель
- Сезонные шаблоны
- История меню
- Экспорт JSON

**Фаза 7 — Продвинутые функции:**
- AI-генерация меню
- Нутриенты: базовый расчёт КБЖУ
- Фото блюд: PWA camera API
- Шаринг: QR-код для синхронизации
- Telegram-бот: уведомления и список покупок
- Семейные профили: настройка диет-тегов

---

## Пользовательские сценарии

### Сценарий 1: Планирование меню на неделю

1. Пользователь открывает приложение
2. Видит меню на текущую неделю (7 дней × 4 приёма)
3. Тапает на день → видит детальный вид с блюдами по приёмам
4. Тапает на блюдо → открывается SwapModal
5. Выбирает другой рецепт из каталога
6. Блюдо заменяется в меню
7. Меню сохраняется автоматически (IndexedDB → Neon)

### Сценарий 2: Генерация списка покупок

1. Пользователь планирует меню на неделю
2. Переходит на страницу "Список покупок"
3. Приложение автоматически генерирует список из всех ингредиентов запланированных блюд
4. Ингредиенты группируются по категориям (мясо, молочка, овощи, крупы)
5. Пользователь отмечает купленные продукты чекбоксами
6. При покупках список синхронизируется с морозилкой (вычитаются покрытые заготовками ингредиенты)

### Сценарий 3: Заготовки на выходной день

1. Пользователь включает режим повара
2. Переходит на страницу "Заготовки"
3. Видит план заготовок, разбитый по фазам (Час 1: Фарш, Час 2: Параллельно, и т.д.)
4. Каждая фаза содержит список задач с таймерами
5. Пользователь отмечает выполненные задачи
6. Видит прогресс выполнения через progress bar
7. Получает подсказки о параллельных процессах (💡 tips)

### Сценарий 4: Трекер морозилки

1. Пользователь добавляет заготовку в морозилку (выбирает рецепт, указывает количество порций)
2. Приложение автоматически рассчитывает срок годности на основе данных рецепта
3. Пользователь видит список всех заготовок с порциями и сроками
4. При приближении срока годности получает предупреждения
5. При планировании меню может использовать заготовки из морозилки
6. При использовании порций обновляет количество оставшихся порций

### Сценарий 5: Режим повара при готовке

1. Пользователь включает режим повара
2. Выбирает дату и приём пищи
3. Запускает сессию готовки
4. Видит floating overlay внизу экрана с текущим шагом
5. Запускает таймеры для параллельных процессов
6. Получает уведомления о завершении таймеров
7. Отмечает выполненные шаги
8. Видит прогресс готовки

---

## Разработка и деплой

### Установка и запуск

```bash
# Установка зависимостей
npm install

# Разработка
npm run dev

# Сборка
npm run build

# Предпросмотр production сборки
npm run preview
```

### Скрипты

- `dev` — запуск dev-сервера Vite
- `build` — сборка production версии (TypeScript + Vite)
- `preview` — предпросмотр production сборки
- `lint` — линтинг кода (ESLint)
- `seed:db` — seed IndexedDB из seedRecipes.ts и seedMenu.ts
- `seed:supabase` — seed Supabase (если настроен)

### Конфигурация

**vite.config.ts:**
- base: '/' (Vercel)
- Плагины: react, tailwindcss, vite-plugin-pwa
- PWA манифест с иконками и настройками

**tailwind.config.ts:**
- Кастомные цвета из дизайн-системы
- Кастомные шрифты (heading, body, mono)
- Кастомные border-radius и box-shadow

**tsconfig.json:**
- strict mode
- target: ES2020
- module: ESNext
- jsx: react-jsx

### Деплой

**Vercel:**
1. Подключить репозиторий к Vercel
2. Настроить build command: `npm run build`
3. Настроить output directory: `dist`
4. Деплой автоматический при push в main
5. Переменные окружения: `DATABASE_URL` (Neon connection string)

### Миграции БД

**Dexie (IndexedDB)** — версионирование схемы:
- Версия 1: базовая схема (recipes, menus, freezer, shopping)
- Версия 2: добавление prepPlans, cookingSessions, chefSettings
- Версия 3: добавление индекса createdAt для menus
- Версия 4: расширение freezer (portionsRemaining, batchId)

При обновлении версии Dexie автоматически выполняет миграции.

**Neon PostgreSQL** — SQL миграции:
- Схема БД: `supabase/migrations/20250208000001_initial_schema.sql`
- Миграции применяются вручную или через CI/CD
- Структура таблиц соответствует IndexedDB схеме для обеспечения синхронизации

---

## Особенности реализации

### Offline-First подход

Основные данные хранятся в Neon PostgreSQL, IndexedDB (Dexie) используется как локальный кэш. Приложение работает полностью офлайн после первой загрузки благодаря IndexedDB и Service Worker, который кэширует все ресурсы (HTML, CSS, JS, изображения). Синхронизация с Neon происходит в фоне через syncService.ts.

### Seed + IndexedDB

Стартовые рецепты и меню заданы в коде seed (`seedRecipes.ts`, `seedMenu.ts`). При запуске приложения они загружаются в IndexedDB через `syncSeedRecipes`. Основное хранилище — Neon PostgreSQL, IndexedDB — локальный кэш. Синхронизация через `syncService.ts`.

### Двухпользовательская модель

Каждое блюдо может иметь вариации для разных членов семьи (Коля, Кристина, Оба). Визуально отображается через цветные бейджи и полоски.

### Режим повара

Специальный режим для организации процесса готовки:
- Floating overlay внизу экрана
- Таймеры для параллельных процессов
- Чек-листы заготовок с прогрессом
- Подсказки о параллельных процессах

### Дизайн-система "Dimension Kitchen"

Тёмная тема в sci-fi стиле:
- Глубокий космический фон (void)
- Портальный зелёный для интерактивных элементов
- Тёплые "фудовые" цвета для контента о еде
- Sci-fi типографика (Exo 2, Share Tech Mono)
- Декоративные элементы (Portal Blobs, Sector Labels)

### Производительность

- Code Splitting: lazy loading маршрутов
- Tree Shaking: удаление неиспользуемого кода
- Кэширование: Service Worker кэш, IndexedDB для данных
- Минификация: сжатие JavaScript и CSS

### Безопасность

- Данные: Neon PostgreSQL (серверное хранение) + IndexedDB (локальный кэш)
- HTTPS обязателен для PWA функциональности
- Валидация данных на клиенте
- Санитизация пользовательского ввода

---

## Roadmap

### MVP (Версия 1.0) — ✅ Реализовано

- Просмотр меню на неделю
- Замена блюда в меню
- Каталог рецептов
- Карточка рецепта
- Добавление рецепта
- Редактирование рецепта
- Рецепты заготовок и соусов
- PWA / Offline

### MVP-2 — ✅ Реализовано

- Список покупок
- Морозилка
- Чек-лист заготовок
- Режим повара

### Версия 2.0 — 🔜 Планируется

- Расширенная синхронизация Neon ↔ IndexedDB (конфликты, retry)
- Расширенная аналитика
- Шаблоны недель
- Фото блюд

### Версия 3.0 — 🔜 Планируется

- Telegram-бот интеграция
- AI-генерация меню
- Нутриенты и КБЖУ
- Шаринг и совместная работа

---

## Дополнительные ресурсы

### Документация проекта

- `README.md` — краткое описание проекта
- `ARCHITECTURE.md` — детальная архитектура
- `PRD.md` — Product Requirements Document
- `FEATURES.md` — список функций и roadmap
- `STYLEGUIDE.md` — руководство по стилям
- `DESIGN_DICTIONARY.md` — словарь элементов дизайна
- `TECHNOLOGIES.md` — описание технологий
- `DEPLOY_LESSONS.md` — уроки деплоя
- `BUILD_GUIDE.md` — руководство по сборке

### Правила разработки

- `.cursor/rules/design-system.mdc` — правила дизайн-системы (alwaysApply)
- `.cursor/rules/features-and-docs.mdc` — правила документации
- `.cursor/rules/parse-recipes.mdc` — правила парсинга рецептов
- `cursorrules` — глобальные правила Cursor

### Конфигурационные файлы

- `package.json` — зависимости и скрипты
- `vite.config.ts` — конфигурация Vite
- `tailwind.config.ts` — конфигурация Tailwind
- `tsconfig.json` — конфигурация TypeScript
- `.eslintrc.json` — конфигурация ESLint

---

## Контакты и поддержка

**Проект:** SemeynoYeda  
**Репозиторий:** GitHub  
**Лицензия:** MIT

---

*Документ обновлён: Февраль 2026*
