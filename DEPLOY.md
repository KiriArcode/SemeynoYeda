# Инструкция по деплою

> 📚 **Подробные уроки деплоя:** Для детального разбора проблем и их решений см. [DEPLOY_LESSONS.md](./DEPLOY_LESSONS.md)

## Подготовка к деплою

1. Убедитесь, что все зависимости установлены:
   ```bash
   npm install
   ```

2. Проверьте сборку:
   ```bash
   npm run build
   ```

3. Проверьте, что папка `dist` создана и содержит собранные файлы.

## Деплой на GitHub Pages

Приложение автоматически деплоится на GitHub Pages при пуше в ветку `main` через GitHub Actions.

### Настройка GitHub Pages

**ВАЖНО:** GitHub Pages должен быть включен ДО первого деплоя!

1. Перейдите в настройки репозитория на GitHub:
   ```
   https://github.com/KiriArcode/SemeynoYeda/settings/pages
   ```
   (или замените `KiriArcode` на ваш username)

2. В разделе "Pages" → "Build and deployment":
   - **Source:** выберите "GitHub Actions"
   - Сохраните изменения

3. После включения GitHub Actions автоматически создаст environment `github-pages`

4. Если environment не создался автоматически:
   - Перейдите в Settings → Environments
   - Создайте новый environment с именем `github-pages`
   - Не нужно добавлять никаких переменных или секретов

### Ручной деплой

Если автоматический деплой не работает:

1. Соберите проект:
   ```bash
   npm run build
   ```

2. Используйте GitHub CLI или веб-интерфейс для загрузки папки `dist` в ветку `gh-pages`

## Настройка PWA

После деплоя:

1. Создайте иконки для PWA (192x192 и 512x512 пикселей)
2. Поместите их в `public/icons/`
3. Раскомментируйте конфигурацию VitePWA в `vite.config.ts`
4. Обновите пути к иконкам в манифесте

## Проверка деплоя

После деплоя проверьте:

- [ ] Приложение открывается по адресу `https://<username>.github.io/SemeynoYeda/`
- [ ] Все страницы работают корректно
- [ ] Навигация функционирует
- [ ] IndexedDB работает (проверьте в DevTools)

## Troubleshooting

### Основные проблемы и быстрые решения

**1. GitHub Pages 404 Error:**
- Убедитесь, что GitHub Pages включен в настройках репозитория (Source: GitHub Actions)
- Проверьте, что `base: '/SemeynoYeda/'` в `vite.config.ts` заканчивается на `/`
- Убедитесь, что `basename` в `Router.tsx` БЕЗ trailing slash (`/SemeynoYeda`)

**2. Навигация не работает (карточки не кликабельны):**
- Используйте `Link` компоненты из `react-router-dom` вместо `div` с `onClick`
- Проверьте, что `basename` определяется правильно (см. `src/app/Router.tsx`)

**3. Статические файлы не загружаются:**
- Проверьте, что `base` в `vite.config.ts` правильный
- Убедитесь, что пути в `dist/index.html` содержат `/SemeynoYeda/assets/...`

**4. GitHub Actions деплой не работает:**
- Проверьте, что GitHub Pages включен (Settings → Pages → Source: GitHub Actions)
- Убедитесь, что environment `github-pages` существует
- Проверьте permissions в `.github/workflows/deploy.yml`

### Проблемы с базовым путём

Если приложение не загружается, проверьте:
- `base` в `vite.config.ts` соответствует имени репозитория (с trailing slash: `/SemeynoYeda/`)
- `basename` в `Router.tsx` соответствует `base` (БЕЗ trailing slash: `/SemeynoYeda`)

### Проблемы с GitHub Actions

- Проверьте, что workflow файл находится в `.github/workflows/deploy.yml`
- Убедитесь, что у репозитория включены GitHub Pages и Actions
- Проверьте, что `dist/` загружается напрямую, без перемещения файлов

### Подробная документация

Для детального разбора всех проблем и их решений см. **[DEPLOY_LESSONS.md](./DEPLOY_LESSONS.md)** — документ содержит:
- Подробное описание каждой проблемы
- Пошаговые решения с примерами кода
- Чеклист для проверки конфигурации
- Полезные команды для диагностики
