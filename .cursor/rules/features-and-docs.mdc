---
description: Keep feature lists and docs in sync. Apply when adding or changing features, or when editing FEATURE_LIST, FEATURES, ARCHITECTURE, or data docs.
globs: ["FEATURE_LIST.md", "FEATURES.md", "ARCHITECTURE.md", "docs/**/*.md"]
alwaysApply: false
---

# Features and documentation conventions

## Единый источник правды по фичам

- При добавлении или изменении фич обновлять статус в **FEATURES.md** и при необходимости в **FEATURE_LIST.md** (✅ реализовано, 📋 запланировано, 🚧 в разработке).
- Не оставлять FEATURE_LIST полностью в статусе «Запланировано», если фича уже реализована.

## Имена компонентов и страниц

- Актуальный список страниц и компонентов — в **FEATURES.md** (таблица маршрутов и раздел «Компоненты»).
- При упоминании в планах или документации использовать эти имена (WeekOverview, MealSlot, PrepBlock, PrepTaskCard, SwapModal и т.д.).

## Архитектура данных

- **Основная БД**: Neon PostgreSQL (serverless), доступ через Vercel Serverless Functions (`api/` директория).
- **Локальный кэш**: IndexedDB (Dexie) — offline-кэш с синхронизацией через `syncService.ts`.
- **Seed данные**: 57 рецептов в [seedRecipes.ts](src/data/seedRecipes.ts), меню в [seedMenu.ts](src/data/seedMenu.ts). Загружаются в Neon через `scripts/seedNeon.ts`.
- **Runtime**: чтение из IndexedDB (быстро), фоновая синхронизация с Neon каждые 30 секунд. Запись — optimistic в IndexedDB, затем push в Neon.
- Не предполагать наличие JSON-файлов рецептов в `src/data/` кроме кода seed.

## Деплой

- **Vercel** — единственная платформа деплоя. `base: '/'` в vite.config.ts.
- GitHub Pages **НЕ ИСПОЛЬЗУЕТСЯ** (документы BUILD_GUIDE.md, DEPLOY.md, FIX_DEPLOY.md — устарели).

## Неиспользуемые технологии

- **Supabase** — НЕ ИСПОЛЬЗУЕТСЯ. `@supabase/supabase-js` подлежит удалению. Для Auth планируется Neon Auth.
